{
  "name": "camcleans",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist --project-name camcleanscline --branch main --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "resend": "^6.9.3"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.0"
  }
}
